const { Cookbook } = require('../src/cookbook');
const { CookbookCli } = require('../src/cookbook-cli');

describe('CookbookCli', () => {
  describe('Adding recipes', () => {
    it('should accept the recipe information and display the correct message', () => {

    });
  });

  describe('Listing recipes', () => {
    it('should display the correct message listing all of the recipe names', () => {

    });
  });

  describe('Retrieving a recipe', () => {
    it('should display the ingredients required to make the specified recipe', () => {

    });
  });

  describe('Deleting a recipe', () => {
    it('should accept the recipe name and display the correct message', () => {

    });
  });
});
